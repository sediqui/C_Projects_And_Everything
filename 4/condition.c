#include <stdbool.h>
#include <stdio.h>
#include <string.h>

typedef char* string;
  
int main (int argc,string argv[]) {
  // int a=2, b=2;
  // a == b ? printf("Equal\n") : printf("Not equal\n");
  // a != b ? printf("Not equal\n") : printf("Equal\n");

  return 0;
}